// Backward-compatible re-export — actual code moved to @/domains/copilot/server/agent-orchestrator
export { runAgent, runConsensus } from "@/domains/copilot/server/agent-orchestrator";
export type { ChatMessage, AgentResponse, ConsensusResponse } from "@/domains/copilot/server/agent-orchestrator";
