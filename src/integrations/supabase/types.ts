// Backward-compatible re-export — actual code lives in @/shared/supabase/types
export type { Database, Tables, TablesInsert, TablesUpdate, Enums, CompositeTypes, Json, DatabaseWithoutInternals, DefaultSchema } from "@/shared/supabase/types";
export { Constants } from "@/shared/supabase/types";
